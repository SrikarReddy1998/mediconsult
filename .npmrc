# Machine-specific: trust the corporate TLS-interception CA so npm can reach the
# registry from behind the proxy. Path uses forward slashes (npm/Windows accept
# them). Gitignored — do not commit (regenerate the bundle with export-ca.ps1).
cafile=C:/Users/cheerka.reddy/corp-ca-bundle.pem
